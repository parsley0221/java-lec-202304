/*
 *  클래스는 크게 필드(속성), 메서드(행위)
 *  필드(속성) : 명사? 사람을 구분할 때 이름 성별 나이와 같은 것.
 *  메서드(행위) : 동사? 
 */

// 클래스 선언
// [접근한정자] class 클래스명
public class ClassExample01 {
	
	//-- 클래스 정의부 : 이곳 {} 내부 공간
	
	//-- 속성은 객체를 구분하는 것.
	//-- ex) int handCount = 2; // 사람은 손이 두 개다.
	//-- 변수와 상수를 사용합니다. (속성은 "그 객체가 갖는" 데이터이기 때문)
	
	//-- 메서드는 위의 객체가 갖는 행위.
	//-- ex) 숟가락을 들다(); // 그 손으로 숟가락을 들어라.

}
